# ── Dependencies ──
node_modules/
.pnp
.pnp.js

# ── Secrets — never commit these ──
.env
.env.local
.env.*.local
.env.development
.env.production
*.pem
*.key

# ── Build output ──
dist/
build/
out/
.next/
.expo/
*.tsbuildinfo

# ── Logs ──
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Caches ──
.cache/
.parcel-cache/
.eslintcache
.vite/
coverage/

# ── Editors / OS ──
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
desktop.ini

# ── Local database dumps and backups ──
*.sql.bak
*.dump
pg_hba.conf.bak-*

# ── Temporary helper scripts ──
RESET-POSTGRES.bat

# Файлҳои аслии расм ва видео (13 МБ). Нусхаҳои коркардшуда дар Front/src/images
# ва Front/public ҳастанд — инҳо танҳо бойгонӣ.
/Alll Senaria/


# Нусхаи захиравии база: маълумоти корбарон ва калидҳо дорад.
backups/

.claude/
.agents/
skills-lock.json

# Овози санҷишӣ ва кеши mp3 — аз нав сохта мешаванд
ovozi-sanjish/
Back/nest-backend/voice-cache/
